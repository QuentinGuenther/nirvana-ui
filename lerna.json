{
  "packages": ["packages/*"],
  "version": "0.0.3",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "version": {
      "ignoreChanges": ["*.md", "*.stories.@(js|jsx|ts|tsx)"],
      "npmClient": "yarn",
      "message": "chore(release): publish"
    },
    "publish": {
      "npmClient": "yarn"
    }
  }
}
