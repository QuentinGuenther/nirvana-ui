{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "docs"
  ],
  "devDependencies": {
    "jest": "^27.5.1",
    "lerna": "^4.0.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {},
  "scripts": {
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream --parallel",
    "test": "yarn jest --coverage"
  }
}
